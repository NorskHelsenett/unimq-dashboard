# API connection settings
BASE_URL="localhost"
BASE_PORT="8080"

# Log level settings, valid values: -8 (trace), -4 (debug), 0 (info), 4 (warn), 8 (error)
LOG_LEVEL=0

# MongoDB connection settings
MONGODB_HOST="localhost"
MONGODB_PORT="27017"
MONGODB_USERNAME="admin"
MONGODB_PASSWORD="password"
MONGODB_DATABASE="unimq"

# Mongo Express connection settings
MONGO_EXPRESS_USERNAME="test"
MONGO_EXPRESS_PASSWORD="test"

# RabbitMQ connection settings
RABBITMQ_HOST="http://localhost"
RABBITMQ_PORT="15672"
RABBITMQ_USERNAME="guest"
RABBITMQ_PASSWORD="guest"

RABBITMQ_CHANNEL_LIMIT=1000
RABBITMQ_CONNECTION_LIMIT=300
RABBITMQ_QUEUE_LIMIT=150

# Prometheus connection settings
PROMETHEUS_HOST="http://localhost"
PROMETHEUS_PORT="9090"

# Example: https://mirror.gcr.io
DOCKER_MIRROR = ""
COMPOSE_PROJECT_NAME = "unimq-dashboard"

# Email settings
# If EMAIL_SMTP_HOST is empty, email sending will be disabled
EMAIL_SMTP_HOST = ""

# Defaults to 587 if not set
EMAIL_SMTP_PORT = "587" 
EMAIL_SMTP_USERNAME ="" 
EMAIL_SMTP_PASSWORD = ""
EMAIL_FROM_ADDRESS = "unimq@example.com"

# Authentication settings
ODIC_CLIENT_ID = "unimq-dashboard"
ODIC_CLIENT_SECRET = "O8jad9kl1PD1er"
ODIC_URL = "https://localhost:5556/dex"
ODIC_REDIRECT_URL = "http://localhost:8080/api/callback"

USER_GROUPS = "group.name1@local,group.name2@local"
ADMIN_GROUPS = "group.name3@local,group.name4@local"
